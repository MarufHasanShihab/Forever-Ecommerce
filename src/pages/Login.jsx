import { useState } from "react";


const Login = () => {
    const [currentState, setCurrentState] = useState("login")
    return (
        <div>
            THis is login page
        </div>
    );
};

export default Login;